# layer 2 - embedding similarity vs known attack vectors
# phase 2 feature - placeholder for now
# plan: load attacks.jsonl, embed with sentence-transformers,
# cosine sim against incoming prompt, flag if above threshold

def scan(prompt: str, threshold: float = 0.82):
    # TODO phase 2
    # from sentence_transformers import SentenceTransformer, util
    # model = SentenceTransformer('all-MiniLM-L6-v2')
    # ...
    return None
